<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ideaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IdeaApp">
        <activity android:name="Features.Add_Idea"></activity>
        <activity android:name="Components.activity_add_ideea"></activity>
        <activity android:name="Components.Main_display_activity" />
        <activity android:name="Components.pop_up_layout"
            android:theme="@style/Theme.IdeaApp1"></activity>
        <activity android:name="Components.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>